import React from "react";

import "./Splash.scss";

const Splash = (props) => {
  return (
    <div className="splash">
      <button>Start New Game</button>
    </div>
  );
};

export default Splash;
